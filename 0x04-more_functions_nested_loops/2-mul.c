#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: used to accept the first intiger.
 * @b: used to accept the second intiger.
 * Return: result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
