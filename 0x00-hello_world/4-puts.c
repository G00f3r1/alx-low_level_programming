#include<stdio.h>
#include<string.h>

int main(void)
{
	/* this is the first c code */
	char mess[60];

	strcpy(mess, "\"Programming is like building a multilingual puzzle");
	puts(mess);
	return (0);
}
