#include<stdio.h>
#include<string.h>

int main(void)
{
	/* the seconde c code */
	char mess[60];

	strcopy(mess, "with proper grammar, but the outcome is a piece of art,");
	puts(mess);
	return (0);	
}
