#include "main.h"
/**
 * add - adds two integers and returns the result.
 * @n: number to be added.
 * @p: number to be added.
 *
 * Return: sum.
 */
int add(int n, int p)
{
	int sum = n + p;

	return (sum);
}
